define( [
    '../../app'
] , function ( controllers ) {
    controllers.controller( 'TopListCtrl' , [
            '$scope' ,
            function ( $s ) {
                console.log('TopListCtrl');
            }
        ] );
} );